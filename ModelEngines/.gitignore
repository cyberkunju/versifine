# Intermediate datasets + trained artifacts — large, regenerable, never committed.
data/
artifacts/
*.parquet
*.jsonl
*.onnx
*.safetensors
*.bin

# EXCEPTION: the teacher packs ARE the committed artifact (subagent-generated,
# frozen for reproducibility — see teacher/README.md). Keep them in git.
!teacher/packs/
!teacher/packs/*.jsonl
!teacher/teacher_packs.jsonl

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
.ipynb_checkpoints/

# Modal
.modal.toml

# OS
.DS_Store
Thumbs.db
